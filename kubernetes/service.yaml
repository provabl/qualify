apiVersion: v1
kind: Service
metadata:
  name: qualify-backend
  namespace: qualify
  labels:
    app.kubernetes.io/name: qualify
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: qualify
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
