apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: qualify

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml        # copy from secret.yaml.example and fill in values
  - deployment.yaml
  - service.yaml

# Override image tag for a specific release:
# images:
#   - name: ghcr.io/provabl/qualify-backend
#     newTag: v0.1.2
